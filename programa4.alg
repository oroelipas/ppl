algoritmo sumacomb;
ent n, m: entero;
sal suma: entero;
{Prec: n= N AND m = M AND M >= N >0}

var 
    a,b,c : entero;
fvar;

a:=b+c;
b:=0;

{Post: n =N AND suma = SUMATORIO(i=1..N)(M sobre i)}
falgoritmo
