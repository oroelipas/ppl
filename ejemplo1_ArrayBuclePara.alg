algoritmo paraArray;
ent a, n, m: entero;
{Prec: n= N AND m = M AND M >= N >0}

var 
    i: entero;
fvar;

para i:=n hasta m hacer
	para i:=n hasta m hacer
		a:=i;
	fpara;
fpara;

{Post: n =N AND suma = SUMATORIO(i=1..N)(M sobre i)}
falgoritmo;
